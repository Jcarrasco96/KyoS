@model IEnumerable<KyoS.Web.Data.Entities.EmergencyContactEntity>

@if (Model != null)
{
    <div class="col-md-12">
        <div class="panel panel-default">
            <div class="panel-heading">
                <h3 class="panel-title">Emergency Contacts</h3>
            </div>
            <div class="panel-body">
                <table class="table table-hover table-responsive table-striped" id="MyTable">
                    <thead>
                        <tr>
                            <th class="col-md-4">
                                @Html.DisplayNameFor(model => model.Name)
                            </th>
                            <th class="col-md-3">
                                @Html.DisplayNameFor(model => model.Address)
                            </th>
                            <th class="col-md-2" style="text-align:center;">
                                @Html.DisplayNameFor(model => model.Telephone)
                            </th>
                            <th class="col-md-2" style="text-align:center;">
                                @Html.DisplayNameFor(model => model.Email)
                            </th>
                            <th class="col-md-1">
                            </th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model)
                        {
                            <tr>
                                <td>
                                    @Html.DisplayFor(modelItem => item.Name)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.Address)
                                </td>
                                <td align="center">
                                    @Html.DisplayFor(modelItem => item.Telephone)
                                </td>
                                <td align="center">
                                    @Html.DisplayFor(modelItem => item.Email)
                                </td>
                                <td align="right">
                                    <a onclick="showInPopup('@Url.Action("EditModal","EmergencyContacts",new {id = @item.Id},Context.Request.Scheme)','Edit Emergency Contacts')" class="btn btn-warning" title="Edit Emergency Contact"><i class="glyphicon glyphicon-pencil"></i></a>
                                    <button onclick="ConfirmDelete(@item.Id)" class="btn btn-danger deleteItem" title="Delete emergency contact"><i class="glyphicon glyphicon-trash"></i></button>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
}



