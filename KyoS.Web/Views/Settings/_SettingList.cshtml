@model IEnumerable<KyoS.Web.Data.Entities.SettingEntity>

@if (Model != null)
{
    <div class="col-md-12">
        <div class="panel panel-default">
            <div class="panel-heading">
                <h3 class="panel-title">Settings</h3>
            </div>
            <div class="panel-body">
                <table class="table table-hover table-responsive table-striped" id="MyTable">
                    <thead>
                        <tr>
                            <th class="col-md-4">Clinic Name</th>
                            <th class="col-md-2" style="text-align:center;">Mental Health</th>
                            <th class="col-md-1" style="text-align:center;">TCM</th>
                            <th class="col-md-2" style="text-align:center;">New Workdays (weeks)</th>
                            <th class="col-md-1"></th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model)
                        {                            
                            <tr>
                                <td>
                                    @Html.DisplayFor(modelItem => item.Clinic.Name)
                                </td>
                                <td align="center">
                                    @Html.DisplayFor(modelItem => item.MentalHealthClinic)
                                </td>
                                <td align="center">
                                    @Html.DisplayFor(modelItem => item.TCMClinic)
                                </td>
                                <td align="center">
                                    @Html.DisplayFor(modelItem => item.AvailableCreateNewWorkdays)
                                </td>
                                <td align="right">
                                    <a onclick="showInPopup('@Url.Action("Edit", "Settings", new { id = item.Id }, Context.Request.Scheme)','Edit setting')" class="btn btn-warning" title="Edit"><i class="glyphicon glyphicon-pencil"></i></a>
                                </td>
                            </tr>                           
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
}



