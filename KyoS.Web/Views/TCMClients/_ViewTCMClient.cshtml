@model IEnumerable<KyoS.Web.Data.Entities.TCMClientEntity>

@if (Model != null)
{
    <div class="col-md-12">
        @if (ViewBag.Delete == "N")
        {
            <div class="alert alert-block alert-danger">
                <button type="button" class="close" data-dismiss="alert">
                    <i class="icon-remove"></i>
                </button>
                <strong class="red">
                    Error. It is not posible delete the TCM case
                </strong>
            </div>
        }
    </div>

     <div class="col-md-12">
         <div class="panel panel-default">
             <div class="panel-heading">
                <h3 class="panel-title">TCM Case</h3>
             </div>
             <div class="panel-body">
                 <table class="table table-hover table-responsive table-striped" id="MyTable">
                     <thead>
                         <tr>
                             <th class="col-md-2">
                                 Casemanager
                             </th>
                             <th class="col-md-2">
                                 Client Name
                             </th>
                             <th class="col-md-2">
                                 Date Open
                             </th>
                             <th class="col-md-2">
                                 Date Close
                             </th>
                             <th class="col-md-2">
                                 Status
                             </th>
                             <th class="col-md-2">
                             </th>
                         </tr>
                     </thead>
                     <tbody>
                        @foreach (var item in Model)
                         {
                         <tr>
                             <td>
                                 @Html.DisplayFor(modelItem => item.Casemanager.Name)
                             </td>
                             <td>
                                 @if (item.Client.Gender == KyoS.Common.Enums.GenderType.Female)
                                 {
                                     <i class="glyphicon glyphicon-user btn-danger"></i>
                                 }
                                 @if (item.Client.Gender == KyoS.Common.Enums.GenderType.Male)
                                 {
                                     <i class="glyphicon glyphicon-user btn-primary"></i>
                                 }
                                 &nbsp;
                                 @Html.DisplayFor(modelItem => item.Client.Name)
                             <td>
                                 @Html.DisplayFor(modelItem => item.DataOpen)
                             </td>
                             <td>
                                 @Html.DisplayFor(modelItem => item.DataClose)
                             </td>
                             <td>
                                 @if (item.Status == KyoS.Common.Enums.StatusType.Open)
                                 {
                                     <span class="label label-success">
                                         Open
                                     </span>
                                 }
                                 @if (item.Status == KyoS.Common.Enums.StatusType.Close)
                                 {
                                     <span class="label label-default">
                                         Close
                                     </span>
                                 }

                             </td>
                             <td align="right">
                                 @if (item.Status == KyoS.Common.Enums.StatusType.Open && User.IsInRole("Manager"))
                                 {
                                     <a onclick="showInPopup('@Url.Action("Edit", "TCMClients", new { id = item.Id }, Context.Request.Scheme)','Edit TCM Case')" class="btn btn-warning" title="Edit TCM Case"><i class="glyphicon glyphicon-pencil"></i></a>
                                 }
                                 @if (item.Status == KyoS.Common.Enums.StatusType.Open && User.IsInRole("CaseManager"))
                                 {
                                     <a asp-action="Create" asp-controller="TCMServicePlans" asp-route-id="@item.Id" class="btn btn-info" title="Create Sewrvice Plan"><i class="glyphicon glyphicon-align-justify"></i></a>
                                 }
                             </td>
                         </tr>
                         }
                     </tbody>
                 </table>
             </div>
         </div>
     </div>
}

