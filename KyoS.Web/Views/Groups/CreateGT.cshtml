@model KyoS.Web.Models.GroupViewModel

@{
    ViewData["Title"] = "Create Group-Therapy Group";
}

<link rel="stylesheet" href="~/css/prism.css">
<link rel="stylesheet" href="~/css/chosen.css">

<div class="breadcrumbs" id="breadcrumbs">
    <ul class="breadcrumb">
        <li>
            <i class="icon-home"></i>
            @Html.ActionLink("Group-Therapy Group", "Group", "Groups")
        </li>
        <li class="active">Create GT Group</li>
    </ul><!--.breadcrumb-->
</div>

<h4>Create GT Group</h4>
<hr />
<div class="row">
    <div class="col-md-12">
        @if (ViewBag.Creado == "Y")
        {
            <div class="alert alert-block alert-success">
                <button type="button" class="close" data-dismiss="alert">
                    <i class="icon-remove"></i>
                </button>
                <i class="icon-ok green"></i>
                <strong class="green">
                    GT Group been successfully created
                </strong>

            </div>
        }
        @if (ViewBag.Creado == "E")
        {
            <div class="alert alert-block alert-warning">
                <button type="button" class="close" data-dismiss="alert">
                    <i class="icon-remove"></i>
                </button>
                <i class="icon-ok red"></i>
                <strong class="red">
                    Error. Group already exist
                </strong>
            </div>
        }
    </div>
    <div>
        <form asp-action="CreateGT" onsubmit="return false;" id="form1">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Id" />
            <partial name="_GroupsGT" />
            <div class="form-group col-md-12"></div>
            <div class="col-md-12 form-group">
                <strong>Note: </strong> Clients notes will be generated based on work weeks of this therapy.
            </div>
            <div class="form-group col-md-12"></div>
            <div class="form-group col-md-12">
                <input type="submit" value="Save" class="btn btn-primary" id="btnSave"/>
                <a asp-action="Group" asp-route-all="@ViewData["all"]" class="btn btn-success">Back</a>
            </div>
        </form>
    </div>
</div>



@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}

    <script src="~/js/chosen.jquery.js" type="text/javascript"></script>
    <script src="~/js/prism.js" type="text/javascript" charset="utf-8"></script>
    <script src="~/js/init.js" type="text/javascript" charset="utf-8"></script>

    <script type="text/javascript">
        $(document).ready(function () {
            $("#btnSave").click(function () {
                if (($("#cbFacilitator option:selected").text() != "[Select facilitator...]") && ($("#cbSchedule option:selected").text() != "[Select Schedule...]")) {
                    $("#btnSave").prop('disabled', true);
                    form1.submit();
                }
            });
        });
    </script>
}