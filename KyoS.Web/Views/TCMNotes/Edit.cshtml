@model KyoS.Web.Models.TCMNoteViewModel
@{
    ViewData["Title"] = "TCM Notes";
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
</head>
<body>

    <div class="breadcrumbs" id="breadcrumbs">
        <ul class="breadcrumb">
            <li>
                <i class="icon-home"></i>
                @if (@ViewData["origin"].ToString() == "0")
                {
                    @Html.ActionLink("TCM Notes For Client", "TCMNotesForCase", "TCMNotes", new { idTCMClient = @Model.IdTCMClient })
                }
                else
                {
                    @Html.ActionLink("Notes in Edition", "NotesStatus", "TCMNotes", new {status = KyoS.Common.Enums.NoteStatus.Edition })
                }

            </li>
            <li class="active">TCM Notes</li>
        </ul><!--.breadcrumb-->
    </div>


    <div class="row">
        <form asp-action="Edit" asp-route-id="@Model.Id" enctype="multipart/form-data">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="IdTCMClient" />
            <input type="hidden" asp-for="IdTCMNote" />
            <input type="hidden" asp-for="CaseManagerDate" />
            <input type="hidden" asp-for="TCMClient.Casemanager.Name" />
            <input type="hidden" asp-for="CreatedOn" />
            <input type="hidden" asp-for="CreatedBy" />
            <partial name="_TCMNotes" />
            <div class="col-md-push-12">                
                <div class="col-md-12 form-group">
                    <a onclick="showInPopup('@Url.Action("CreateNoteActivity", "TCMNotes",new {startTime = @Model.DateOfService, idNote = @Model.Id, idDomain = 103, idTCMClient = Model.IdTCMClient}, Context.Request.Scheme)','Create TCM Note')" class="btn btn-success" title="Create TCM Note"><i class="glyphicon glyphicon-plus"></i> Add Service</a>
                </div>
                <div id="view-NoteActivity">
                    @await Html.PartialAsync("_ViewTCMNoteActivity", Model.TCMNoteActivity.ToList())
                </div>                               
            </div>
            <partial name="_TCMNotes1" />
            <div class="form-group col-md-12">
                <div class="form-group col-md-2">
                    &nbsp;
                </div>
                <div class="form-group col-md-8">
                    <input type="submit" value="Save" asp-route-origin="@ViewData["origin"]" class="btn btn-primary" />
                    @if (@ViewData["origin"].ToString() == "0")
                    {
                        <a asp-action="TCMNotesForCase" asp-route-idTCMClient="@Model.IdTCMClient" class="btn btn-success">Back</a>
                    }
                    else
                    { 
                        <a asp-action="NotesStatus" asp-route-status="@KyoS.Common.Enums.NoteStatus.Edition" class="btn btn-success">Back</a>
                    }

                </div>
                <div class="form-group col-md-2">
                    &nbsp;
                </div>
            </div>
        </form>
    </div>
</body>
</html>

<partial name="_ModalPopup" />
<partial name="_DeleteDialog" />

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
    <script src="~/js/jquery.dataTables.min.js"></script>

    <script type="text/javascript">

    </script>
}

