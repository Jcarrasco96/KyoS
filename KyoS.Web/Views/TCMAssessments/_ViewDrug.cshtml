@model IEnumerable<KyoS.Web.Data.Entities.TCMAssessmentDrugEntity>
<div class="col-push-12">
    <div class="col-md-12">
        <div class="panel panel-default">
            <div class="panel-heading">
                @*<h3 class="panel-title">*@
                @*</h3>*@
            </div>
            <div class="panel-body">
                <table class="table table-hover table-responsive table-striped" id="MyTable">
                    <thead>
                        <tr>
                            <th class="col-md-2">
                                Type of Drug/Substance
                            </th>
                            <th class="col-md-2" style="text-align:center;">
                                Date Begin
                            </th>
                            <th class="col-md-2" style="text-align:center;">
                                Age
                            </th>
                            <th class="col-md-2" style="text-align:center;">
                                Frequency
                            </th>
                            <th class="col-md-2" style="text-align:center;">
                                Last Time Used
                            </th>
                            <th class="col-md-2">

                            </th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model)
                        {
                            <tr>
                                <td>
                                    @Html.DisplayFor(modelItem => item.SustanceName)
                                </td>
                                <td align="center">
                                    @Html.DisplayFor(modelItem => item.DateBegin)
                                </td>
                                <td align="center">
                                    @Html.DisplayFor(modelItem => item.Age)
                                </td>
                                <td align="center">
                                    @Html.DisplayFor(modelItem => item.Frequency)
                                </td>
                                <td align="center">
                                    @Html.DisplayFor(modelItem => item.LastTimeUsed)
                                </td>
                                <td align="right">
                                    @if (User.IsInRole("CaseManager"))
                                    { 
                                        <a onclick="showInPopup('@Url.Action("EditDrugModal", "TCMAssessments", new { id = @item.Id }, Context.Request.Scheme)','Edit Behavioral Health')" class="btn btn-warning" title="Edit Behavioral Health"><i class="glyphicon glyphicon-pencil"></i></a>
                                    }
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>

