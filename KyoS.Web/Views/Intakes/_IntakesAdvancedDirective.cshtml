@model KyoS.Web.Models.IntakeAdvancedDirectiveViewModel

<div class="col-md-push-12">
    <div class="form-group col-md-4">
        <label asp-for="Client.Name" class="control-label">Name of Recipient</label>
        <input asp-for="Client.Name" class="form-control" readonly>
        <span asp-validation-for="Client.Name" class="text-danger"></span>
    </div>
    <div class="form-group col-md-2">
        <label asp-for="Client.Code" class="control-label">Case No.</label>
        <input asp-for="Client.Code" class="form-control" readonly>
        <span asp-validation-for="Client.Code" class="text-danger"></span>
    </div>
</div>
<div class="col-md-12">
    <hr />
</div>
@if (User.IsInRole("Manager") || User.IsInRole("Frontdesk") || User.IsInRole("Documents_Assistant")) 
{
  <div class="col-md-12">
    <pre>
    <strong>MENTAL HEALTH ADVANCE DIRECTIVE ACKNOWLEDGEMENT</strong> 
    <b>(ADULT CLIENTS ONLY)</b>

    A <i>Mental Health Advance Directive</i> is a legal document that lets you name another individual as agent to make health care decisions for you if you become incapable of making
    your own decisions, or if you want someone else to make those decisions for you even though you are still capable.
    <strong>@Html.DisplayFor(modelItem => Model.Client.Clinic.Name)</strong> does not require you to have a Mental Health Advance Directive, however <strong>@Html.DisplayFor(modelItem => Model.Client.Clinic.Name)</strong> will follow the terms of any 
    Advance Directive that you may have executed, to the extent permitted by the law.
    If you already have a Mental Health Advance Directive, please provide the agency with a copy.

    <input type="checkbox" asp-for="IHave" class="form-check-input" id="check1"> I HAVE executed a Mental Health Advance Directive and will provide the agency with a copy. The name and contact information for my agent or surrogate is: 
    
        <input asp-for="Client.EmergencyContact.Name" readonly style="width:20%">     <input asp-for="Client.EmergencyContact.Telephone" readonly style="width:15%">     <input asp-for="Client.RelationShipOfEmergencyContact" readonly style="width:15%">
        <strong>Name</strong>                                     <strong>Phone Number</strong>                     <strong>Relationship</strong> 
    
    <input type="checkbox" asp-for="IHaveNot" class="form-check-input" id="check101"> I HAVE NOT executed a Mental Health Advance Directive, however I have read or been explained, and understand my rights to have a Mental Health Advance Directive.
    </pre>
  </div>
  <div class="col-md-12">
    <pre>
    @if (Model.Client.LegalGuardian.Name != "")
    {
    <strong>Person served signature date</strong>    <input asp-for="DateSignaturePerson">
    <strong>(@Html.DisplayFor(modelItem => Model.Client.Name))</strong>
    }
    else
    { 
    <strong>Legal Guardian signature date</strong>    <input asp-for="DateSignatureLegalGuardian">
    <strong>(@Html.DisplayFor(modelItem => Model.Client.LegalGuardian.Name))</strong>
    }

    @if (Model.Id == 0)
    {
    <input type="checkbox" class="form-check-input" disabled> <i>Signed</i>
    }
    else
    { 
    <input type="checkbox" class="form-check-input" checked disabled> <i>Signed</i>
    }      
    <strong>Employee signature date</strong>         <input asp-for="DateSignatureEmployee">
    <strong>(@Html.DisplayFor(modelItem => Model.AdmissionedFor))</strong>                                                                    
    </pre>
  </div>
  <div class="col-md-12">
      &nbsp;
  </div>
  <div>
    <div class="form-group col-md-12">
        <input type="checkbox" asp-for="Documents" class="form-check-input" id="Documents"> Documents were verbally translated to me in my native language.<br />
        <label asp-for="Documents" class="forgot">
            &nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;<strong>@Html.DisplayFor(modelItem => Model.Client.Clinic.Name)</strong>
        </label>
    </div>
  </div>
  <br />
}
else
{
    <div class="col-md-12">
        <pre>
        <strong>MENTAL HEALTH ADVANCE DIRECTIVE ACKNOWLEDGEMENT</strong> 
        <b>(ADULT CLIENTS ONLY)</b>

    A <i>Mental Health Advance Directive</i> is a legal document that lets you name another individual as agent to make health care decisions for you if you become incapable of making
    your own decisions, or if you want someone else to make those decisions for you even though you are still capable.
        <strong>@Html.DisplayFor(modelItem => Model.Client.Clinic.Name)</strong> does not require you to have a Mental Health Advance Directive, however <strong>@Html.DisplayFor(modelItem => Model.Client.Clinic.Name)</strong> will follow the terms of any 
    Advance Directive that you may have executed, to the extent permitted by the law.
    If you already have a Mental Health Advance Directive, please provide <strong>@Html.DisplayFor(modelItem => Model.Client.Clinic.Name)</strong> with a copy.

        <input type="checkbox" asp-for="IHave" class="form-check-input" disabled>  I HAVE executed a Mental Health Advance Directive and will provide <strong>@Html.DisplayFor(modelItem => Model.Client.Clinic.Name)</strong> 
    
        <input asp-for="Client.EmergencyContact.Name" disabled style="width:20%">     <input asp-for="Client.EmergencyContact.Telephone" disabled style="width:20%">     <input asp-for="Client.RelationShipOfEmergencyContact" disabled style="width:20%">
        <strong>Name</strong>                                     <strong>Phone Number</strong>                     <strong>Relationship</strong> 
    
        <input type="checkbox" asp-for="IHaveNot" class="form-check-input" disabled> I HAVE NOT executed a Mental Health Advance Directive, however I have read or been explained, and understand my rights to have a Mental Health Advance Directive.
    </pre>
    </div>
    <div class="col-md-12">
    <pre>
    @if (Model.Client.LegalGuardian.Name != "")
    {
    <strong>Person served signature date</strong>    <input asp-for="DateSignaturePerson" disabled>
    <strong>(@Html.DisplayFor(modelItem => Model.Client.Name))</strong>
    }
    else
    { 
    <strong>Legal Guardian signature date</strong>    <input asp-for="DateSignatureLegalGuardian" disabled>
    <strong>(@Html.DisplayFor(modelItem => Model.Client.LegalGuardian.Name))</strong>
    }

    @if (Model.Id == 0)
    {
    <input type="checkbox" class="form-check-input" disabled> <i>Signed</i>
    }
    else
    { 
    <input type="checkbox" class="form-check-input" checked disabled> <i>Signed</i>
    }
    <strong>Employee signature date</strong>         <input asp-for="DateSignatureEmployee" disabled>
    <strong>(@Html.DisplayFor(modelItem => Model.AdmissionedFor))</strong>                                                                    
    </pre>
    </div>
    <div class="col-md-12">
        &nbsp;
    </div>
    <div>
        <div class="form-group col-md-12">
            <input type="checkbox" asp-for="Documents" class="form-check-input" id="Documents" disabled> Documents were verbally translated to me in my native language.<br />
            <label asp-for="Documents" class="forgot">
                &nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;<strong>@Html.DisplayFor(modelItem => Model.Client.Clinic.Name)</strong>
            </label>
        </div>
    </div>
    <br />
}



