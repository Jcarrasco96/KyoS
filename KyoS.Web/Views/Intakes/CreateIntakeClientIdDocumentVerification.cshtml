@model KyoS.Web.Models.IntakeClientIdDocumentVerificationViewModel
@{
    ViewData["Title"] = "Documents Verification";
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
</head>
<body>

    <div class="breadcrumbs" id="breadcrumbs">
        <ul class="breadcrumb">
            <li>
                <i class="icon-home"></i>
                @if (User.IsInRole("CaseManager"))
                {
                    @Html.ActionLink("TCM Intake Section 1", "TCMIntakeSectionDashboard", "TCMIntakes", new { id = @Model.IdTCMClient, section = 1, origin = ViewData["origi"] })
                }
                else
                {
                    @Html.ActionLink("TCM Intake Section 1", "TCMIntakeSectionDashboardReadOnly", "TCMIntakes", new { id = @Model.IdTCMClient, section = 1, origin = ViewData["origi"] })
                }
            </li>
            <li class="active">
                ID Documents Verification Form
            </li>
        </ul><!--.breadcrumb-->
    </div>

    <h4>
        ID Documents Verification Form
    </h4>
    <hr />
    <div class="row">
        <form asp-action="CreateMIntakeClientIdDocumentVerification" asp-route-id="@Model.Id" asp-route-origi="@ViewData["origi"]" enctype="multipart/form-data">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="IdClient" />
            <input type="hidden" asp-for="CreatedOn" />
            <input type="hidden" asp-for="CreatedBy" />
            <input type="hidden" asp-for="Client_FK" />
            <input type="hidden" asp-for="AdmissionedFor" />
            <partial name="_TCMIntakeClientIdDocumentVerification" />
            <div class="form-group col-md-12">
                <input type="submit" value="Save" class="btn btn-primary" disabled />
                <a asp-action="TCMIntakeSectionDashboardReadOnly" asp-route-id="@Model.Client.Id" asp-route-section="1" asp-route-origin="@ViewData["origi"]" class="btn btn-success">Back</a>
            </div>
        </form>
    </div>
</body>
</html>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
    <script src="~/js/jquery.dataTables.min.js"></script>

    <script type="text/javascript">

    </script>
}



