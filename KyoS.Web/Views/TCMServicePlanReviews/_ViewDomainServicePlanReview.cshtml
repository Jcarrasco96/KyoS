@model KyoS.Web.Models.TCMServicePlanReviewViewModel

<div class="panel panel-default">
    <div class="panel-heading">
        <h3 class="panel-title">TCM Service Plan Review</h3>
    </div>
    <div class="panel-body">
        <table class="table table-hover table-responsive table-striped">
            <thead>
                <tr>
                    <th class="col-md-1" align="center">
                        Code
                    </th>
                    <th class="col-md-2" align="center">
                        Name
                    </th>
                    <th class="col-md-1" align="center">
                        Status
                    </th>
                    <th class="col-md-2" align="center">
                        Objectives Status / Objectives Name
                    </th>
                    <th class="col-md-4" align="center">
                        Objectives Changes/Updates
                    </th>
                </tr>
            </thead>
            <tbody>
                @for (int i = 0; i < Model.Domain_List.Count(); i++)
                {
                    <tr>
                        <td>
                            <input asp-for="@Model.Domain_List[i].Code" class="form-control" readonly />
                            @Model.Domain_List[i].Addendum_ServicePlan
                        </td>
                        <td>
                            <input asp-for="@Model.Domain_List[i].Name" class="form-control" readonly />
                        </td>
                        <td>
                            <div class="form-group">
                                <select asp-for="@Model.Domain_List[i].Status" asp-items="Model.StatusListDomain" class="form-control"></select>
                                <span asp-validation-for="@Model.Domain_List[i].Status" class="text-danger"></span><br />
                                <input asp-for="@Model.Domain_List[i].ID" readonly />
                            </div>
                        </td>
                        <td>
                            @if (Model.Domain_List[i].ObjectiveList.Count() > 0)
                            {
                                @for (int j = 0; j < Model.Domain_List[i].ObjectiveList.Count(); j++)
                                {
                                    <input asp-for="@Model.Domain_List[i].ObjectiveList[j].ID" readonly />
                                    <select asp-for="@Model.Domain_List[i].ObjectiveList[j].Status" asp-items="Model.StatusListObjetive" class="form-control">@Model.Domain_List[i].ObjectiveList[j].Name</select>
                                    <span asp-validation-for="@Model.Domain_List[i].ObjectiveList[j].Status" class="text-danger"></span>

                                    <input asp-for="@Model.Domain_List[i].ObjectiveList[j].Name" class="form-control" readonly />
                                }
                            }
                            else
                            {
                                <a onclick="showInPopup('@Url.Action("CreateObjetive", "TCMServicePlanReviews", new { id = Model.Domain_List[i].ID}, Context.Request.Scheme)','Objetive')" class="btn btn-warning" title="Objetive"><i class="glyphicon glyphicon-plus"></i>Objetive</a>
                            }

                        </td>
                        <td><input asp-for="@Model.Domain_List[i].Updates_Changes" class="form-control" /></td>

                    </tr>
                }
            </tbody>
        </table>
    </div>

</div>

<partial name="_ModalPopup" />
@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
    <script src="~/js/jquery.dataTables.min.js"></script>

    <script type="text/javascript">

    </script>
}


