@model KyoS.Web.Models.TCMIntakeForeignLanguageViewModel
@{
    ViewData["Title"] = "Foreign Language Acknowledgement";
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
</head>
<body>

    <div class="breadcrumbs" id="breadcrumbs">
        <ul class="breadcrumb">
            <li>
                <i class="icon-home"></i>
                @Html.ActionLink("TCM Intakes", "TCMIntakeDashboard", "TCMIntakes", new { id = Model.TcmClient_FK })
            </li>
            <li class="active">
                Foreign Language Acknowledgement
            </li>
        </ul><!--.breadcrumb-->
    </div>

    <h4>
        Foreign Language Acknowledgement
    </h4>
    <hr />
    <div class="row">
        <form asp-action="CreateTCMForeignLanguage" asp-route-id="@Model.Id" enctype="multipart/form-data">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="IdTCMClient" />
            <input type="hidden" asp-for="TcmClient_FK" />
            <input type="hidden" asp-for="AdmissionedFor" />
            <partial name="_TCMIntakeForeignLanguage" />
            <div class="form-group col-md-12">
                <input type="submit" value="Save" class="btn btn-primary" />
                <a asp-action="TCMIntakeDashboard" asp-route-id="@Model.TcmClient.Id" class="btn btn-success">Back</a>
            </div>
        </form>
    </div>
</body>
</html>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
    <script src="~/js/jquery.dataTables.min.js"></script>

    <script type="text/javascript">

    </script>
}




