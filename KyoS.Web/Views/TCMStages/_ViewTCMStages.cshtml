@model IEnumerable<KyoS.Web.Data.Entities.TCMStageEntity>

@if (Model != null)
{
    <div class="col-md-12">
        @if (ViewBag.Delete == "N")
        {
            <div class="alert alert-block alert-danger">
                <button type="button" class="close" data-dismiss="alert">
                    <i class="icon-remove"></i>
                </button>
                <strong class="red">
                    Error. It is not posible delete the TCM Stage
                </strong>
            </div>
        }
    </div>

    <div class="col-md-12">
        <div class="panel panel-default">
            <div class="panel-heading">
                <h3 class="panel-title">TCM Stages</h3>
            </div>
            <div class="panel-body" id="view-tcmStage">
                <table class="table table-hover table-responsive table-striped" id="MyTable">
                    <thead>
                        <tr>
                            <th class="col-md-2" align="center">
                                Code
                            </th>
                            <th class="col-md-2" align="center">
                                Stage
                            </th>
                            <th class="col-md-5" align="center">
                                Stage Name
                            </th>
                            <th class="col-md-1" align="center">
                                Units
                            </th>
                            <th class="col-md-2" align="center">
                            </th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model)
                        {
                        <tr>
                            <td>
                       e     @Html.DisplayFor(modelItem => item.tCMservice.Code)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.ID_Etapa)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.Name)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.Units)
                            </td>
                            <td align="right">
                                <a onclick="showInPopup('@Url.Action("Edit", "TCMStages", new { id = item.Id }, Context.Request.Scheme)','Edit TCM Stage')" class="btn btn-warning" title="Edit"><i class="glyphicon glyphicon-pencil"></i></a>
                                <button data-id="@item.Id" class="btn btn-danger deleteItem" data-toggle="modal" data-target="#deleteDialog"><i class="glyphicon glyphicon-trash"></i></button>
                            </td>
                        </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>

}


